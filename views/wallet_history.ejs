<!DOCTYPE html>
<html>
<head>
  <title>Histori Transaksi - <%= name %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-light">
  <div class="container mt-4">
    <h1>📜 Histori Transaksi: <%= name %></h1>

    <% if (history.length === 0) { %>
      <p>Belum ada transaksi untuk wallet ini.</p>
    <% } else { %>
      <table class="table table-dark table-bordered mt-3">
        <thead>
          <tr>
            <th>Blok</th>
            <th>Dari</th>
            <th>Ke</th>
            <th>Jumlah</th>
            <th>Waktu</th>
          </tr>
        </thead>
        <tbody>
          <% history.forEach(tx => { %>
            <tr>
              <td><%= tx.blockIndex %></td>
              <td><%= tx.from %></td>
              <td><%= tx.to %></td>
              <td><%= tx.amount %></td>
              <td><%= new Date(tx.timestamp).toLocaleString() %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>

    <a href="/wallet/<%= name %>" class="btn btn-outline-light mt-4">⬅️ Kembali ke Wallet</a>
  </div>
</body>
</html>
