<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Wallet Detail — JSChain</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- Bootstrap & FontAwesome -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <style>
    body { background: #f5f7fa; font-family: 'Inter', sans-serif; }
    .navbar { background-color: #fff; border-bottom: 1px solid #ddd; }
    h1, h4 { font-weight: 600; }
    .card-light { background: #fff; border: 1px solid #ddd; border-radius: 12px; box-shadow: 0 2px 6px rgba(0,0,0,0.05); margin-bottom: 1.5rem; }
    .mono { font-family: monospace; word-break: break-all; }
    .btn-primary { background-color: #1877F2; border-color: #1877F2; font-weight: 600; }
    .btn-primary:hover { background-color: #145dbf; border-color: #145dbf; }
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light px-3">
    <a class="navbar-brand fw-bold" href="/">⚡ JSChain Wallets</a>
    <div class="ms-auto">
      <a class="btn btn-outline-info me-2" href="/miner">Miner</a>
      <a class="btn btn-outline-dark me-2" href="/explorer">Explorer</a>
      <a class="btn btn-outline-warning me-2" href="/stats">Stats</a>
      <a class="btn btn-outline-danger" href="/logout">Logout</a>
    </div>
  </nav>

  <div class="container my-5">
    <h1 class="mb-4">🔎 Wallet Detail</h1>

    <div class="card-light p-4">
      <h4 class="mb-3"><i class="fas fa-wallet text-success"></i> <%= name %></h4>

      <div class="mb-3">
        <label class="form-label fw-bold">Address</label>
        <div class="mono"><%= address %>
          <button class="btn btn-sm btn-outline-info ms-2" onclick="navigator.clipboard.writeText('<%= address %>')">Copy</button>
        </div>
      </div>

      <div class="mb-3">
        <label class="form-label fw-bold">Public Key</label>
        <div class="mono"><%= publicKey %></div>
      </div>

      <div class="d-flex flex-wrap gap-2 mt-4">
        <a class="btn btn-primary" href="/wallets/<%= encodeURIComponent(name) %>/history">
          <i class="fas fa-list"></i> Transaction History
        </a>
        <a class="btn btn-outline-warning" href="/wallets/<%= encodeURIComponent(name) %>/export">
          <i class="fas fa-file-export"></i> Export Wallet
        </a>
        <form action="/wallets/<%= encodeURIComponent(name) %>/delete" method="POST" onsubmit="return confirm('Yakin hapus wallet <%= name %>?')">
          <button class="btn btn-outline-danger"><i class="fas fa-trash"></i> Delete</button>
        </form>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
