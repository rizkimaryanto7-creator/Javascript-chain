<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Kirim Transaksi | JavaScript-chain Explorer</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #ffffff;
      font-family: 'Inter', sans-serif;
    }
    .card {
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      border-radius: 10px;
    }
    .btn-primary {
      background-color: #1877F2;
      border-color: #1877F2;
    }
    .btn-primary:hover {
      background-color: #145dbf;
      border-color: #145dbf;
    }
  </style>
</head>
<body>

  <div class="container my-5">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="card p-4">
          <h2 class="text-center mb-4">🚀 Kirim Transaksi</h2>
          <form method="POST" action="/send">
            <div class="mb-3">
              <label for="from" class="form-label">Nama Wallet Pengirim</label>
              <input type="text" id="from" name="from" class="form-control" placeholder="Nama wallet pengirim" required>
            </div>
            <div class="mb-3">
              <label for="to" class="form-label">Public Key Tujuan</label>
              <input type="text" id="to" name="to" class="form-control" placeholder="Public key tujuan" required>
            </div>
            <div class="mb-3">
              <label for="amount" class="form-label">Jumlah (javascript)</label>
              <input type="number" id="amount" name="amount" class="form-control" placeholder="Amount JavaScript" required>
            </div>
            <button type="submit" class="btn btn-primary w-100">Kirim Transaksi</button>
          </form>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
