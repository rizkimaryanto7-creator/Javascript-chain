<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Transaction — JS-Chain</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background:#121212; color:#fff }
    .mono { font-family: monospace; word-break: break-all }
  </style>
</head>
<body class="p-4">
<div class="container">
  <h1>🔗 Transaction Detail</h1>
  <div class="card bg-dark mb-4">
    <div class="card-body">
      <div><strong>TX Hash:</strong> <span class="mono text-info"><%= hash %></span></div>
      <div><strong>Block:</strong> <a href="/block/<%= blockIndex %>" class="text-warning">#<%= blockIndex %></a></div>
      <div><strong>From:</strong> <span class="mono"><%= tx.from %></span></div>
      <div><strong>To:</strong> <span class="mono"><%= tx.to %></span></div>
      <div><strong>Amount:</strong> <%= tx.amount %></div>
      <div><strong>Timestamp:</strong> <%= new Date(tx.timestamp).toLocaleString() %></div>
      <% if (tx.signature) { %>
        <div><strong>Signature:</strong> <span class="mono text-muted"><%= tx.signature %></span></div>
      <% } %>
    </div>
  </div>
  <a class="btn btn-outline-info" href="/explorer">Back to Explorer</a>
</div>
</body>
</html>
