<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Address Explorer — JS-Chain</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background:#121212; color:#fff }
    .mono { font-family: monospace; word-break: break-all }
  </style>
</head>
<body class="p-4">
<div class="container">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>🧑 Address Explorer</h1>
    <div>
      <a class="btn btn-outline-info me-2" href="/explorer">Explorer</a>
      <a class="btn btn-outline-light" href="/">Home</a>
    </div>
  </div>

  <div class="card bg-dark mb-4">
    <div class="card-body">
      <div><strong>Address:</strong></div>
      <div class="mono text-info"><%= addr %></div>
      <div class="mt-2">Balance: <strong><%= balance %></strong></div>
      <div class="mt-2">Total Transactions: <strong><%= txs.length %></strong></div>
    </div>
  </div>

  <h4>Transactions</h4>
  <table class="table table-dark table-striped">
    <thead>
      <tr>
        <th>Block</th>
        <th>From</th>
        <th>To</th>
        <th>Amount</th>
        <th>Time</th>
      </tr>
    </thead>
    <tbody>
      <% txs.slice().reverse().forEach(tx => { %>
        <tr>
          <td><%= tx.blockIndex %></td>
          <td class="mono"><%= tx.from.slice(0, 12) %>...</td>
          <td class="mono"><%= tx.to.slice(0, 12) %>...</td>
          <td><%= tx.amount %></td>
          <td><%= new Date(tx.timestamp).toLocaleString() %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>
</body>
</html>
