<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Block <%= block.index %> — JS-Chain</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background:#121212; color:#fff }
    .mono { font-family: monospace; word-break: break-all }
  </style>
</head>
<body class="p-4">
<div class="container">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>📦 Block #<%= block.index %></h1>
    <div>
      <a class="btn btn-outline-info me-2" href="/explorer">Explorer</a>
      <a class="btn btn-outline-light" href="/">Home</a>
    </div>
  </div>

  <div class="card bg-dark mb-4">
    <div class="card-body">
      <div><strong>Hash:</strong> <span class="mono text-info"><%= block.hash %></span></div>
      <div><strong>Previous:</strong> <span class="mono text-muted"><%= block.previousHash %></span></div>
      <div><strong>Nonce:</strong> <%= block.nonce %></div>
      <div><strong>Timestamp:</strong> <%= new Date(block.timestamp).toLocaleString() %></div>
    </div>
  </div>

  <h4>Transactions (<%= block.transactions.length %>)</h4>
  <table class="table table-dark table-striped">
    <thead>
      <tr>
        <th>From</th>
        <th>To</th>
        <th>Amount</th>
        <th>Time</th>
      </tr>
    </thead>
    <tbody>
      <% block.transactions.forEach(tx => { %>
        <tr>
          <td class="mono"><a href="/address/<%= tx.from %>" class="text-info"><%= tx.from.slice(0, 12) %>...</a></td>
          <td class="mono"><a href="/address/<%= tx.to %>" class="text-info"><%= tx.to.slice(0, 12) %>...</a></td>
          <td><%= tx.amount %></td>
          <td><%= new Date(block.timestamp).toLocaleString() %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>
</body>
</html>
